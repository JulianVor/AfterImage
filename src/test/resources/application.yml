spring:
  datasource:
    url: jdbc:h2:mem:afterimage;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
  thymeleaf:
    cache: false

afterimage:
  piwigo:
    enabled: false
  admin:
    username: admin
    password: test-password
